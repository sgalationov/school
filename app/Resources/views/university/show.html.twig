{% extends 'base.html.twig' %}

{% block body %}
    <h1>Университет</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ university.id }}</td>
            </tr>
            <tr>
                <th>Название</th>
                <td>{{ university.name }}</td>
            </tr>
            <tr>
                <th>Адрес</th>
                <td>{{ university.address }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('university_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('university_edit', { 'id': university.id }) }}">Edit</a>
        </li>
        <li>
            <a href="{{ path('university_delete', { 'id': university.id }) }}">Delete</a>
        </li>
    </ul>
{% endblock %}
