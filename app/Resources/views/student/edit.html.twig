{% extends 'base.html.twig' %}

{% block body %}
    <h1>Редактор студентов</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Обновить" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('student_index') }}">Назад к списку</a>
        </li>
    </ul>
{% endblock %}
